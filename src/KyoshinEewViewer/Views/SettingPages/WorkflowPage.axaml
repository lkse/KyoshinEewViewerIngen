<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:loc="using:KyoshinEewViewer.Localization"
             xmlns:vm="using:KyoshinEewViewer.ViewModels"
		     xmlns:ui="using:FluentAvalonia.UI.Controls"
             xmlns:services="clr-namespace:KyoshinEewViewer.Services;assembly=KyoshinEewViewer"
             xmlns:workflows="clr-namespace:KyoshinEewViewer.Services.Workflows;assembly=KyoshinEewViewer"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:DataType="vm:SettingWindowViewModel" x:CompileBindings="True"
             x:Class="KyoshinEewViewer.Views.SettingPages.WorkflowPage">
    <UserControl.Styles>
        <Style Selector="Label">
            <Setter Property="Foreground" Value="{DynamicResource MainForegroundColor}" />
        </Style>
        <Style Selector="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>
        <Style Selector="ui|FASettingsExpander">
            <Setter Property="Margin" Value="10,3,10,0" />
        </Style>
        <Style Selector="ScrollViewer">
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled" />
        </Style>
        <Style Selector="TabItem:selected /template/ Border">
            <Setter Property="Background" Value="{DynamicResource DockTitleBackgroundColor}" />
        </Style>
    </UserControl.Styles>
    <Grid RowDefinitions="40,*" Margin="5">
        <Grid ColumnDefinitions="auto,*,auto">
            <StackPanel Spacing="5" Orientation="Horizontal" >
                <Button Command="{Binding OpenWorkflowPage}" Content="{loc:Localize WorkflowOpenGuide}" VerticalAlignment="Stretch" />
                <Button Command="{Binding WorkflowService.SaveWorkflows}" VerticalAlignment="Stretch"
                        ToolTip.Tip="{loc:Localize WorkflowSave}">
                    <ui:FASymbolIcon Symbol="Save" FontSize="18" />
                </Button>
                <Button Command="{Binding LoadWorkflows}" VerticalAlignment="Stretch"
                        ToolTip.Tip="{loc:Localize WorkflowRestore}">
                    <ui:FASymbolIcon Symbol="Undo" FontSize="18" />
                </Button>
            </StackPanel>
            <ComboBox Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="20,2,5,2"
                      IsVisible="{Binding WorkflowService.Workflows.Count}"
                      ItemsSource="{Binding WorkflowService.Workflows}" SelectedValue="{Binding SelectedWorkflow}">
                <ComboBox.ItemTemplate>
                    <DataTemplate x:DataType="workflows:Workflow">
                        <StackPanel Orientation="Horizontal" Spacing="5">
                            <TextBlock Text="{loc:Localize WorkflowDisabledSuffix}" IsVisible="{Binding !Enabled}" />
                            <Panel HorizontalAlignment="Left">
                                <TextBlock Text="{Binding Name}" />
                                <Rectangle IsVisible="{Binding !Enabled}" Height="1" Fill="{DynamicResource MainForegroundColor}" VerticalAlignment="Center"/>
                            </Panel>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Button Command="{Binding AddWorkflow}" VerticalAlignment="Stretch" Grid.Column="2"
                    ToolTip.Tip="{loc:Localize WorkflowCreate}">
                <ui:FASymbolIcon Symbol="Add" FontSize="18" />
            </Button>
        </Grid>
        <StackPanel IsVisible="{Binding !WorkflowService.Workflows.Count}" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="{loc:Localize WorkflowEmpty}" HorizontalAlignment="Center" />
            <TextBlock Text="{loc:Localize WorkflowExperimentalWarning}" Background="{DynamicResource WarningBackgroundColor}" Foreground="{DynamicResource WarningForegroundColor}" Margin="5" />
        </StackPanel>
        <ScrollViewer IsVisible="{Binding SelectedWorkflow, Converter={x:Static ObjectConverters.IsNotNull}}" Grid.Row="1" Margin="0,5,0,0">
            <StackPanel Margin="10,20,10,0">
                <Grid ColumnDefinitions="auto,*,auto">
                    <CheckBox IsChecked="{Binding SelectedWorkflow.Enabled}" />
                    <TextBox Text="{Binding SelectedWorkflow.Name}" Grid.Column="1" />
                    <Button Grid.Column="2" VerticalAlignment="Stretch" Width="30" Padding="5" Margin="5,0,0,0"
                            Command="{Binding RemoveWorkflow}" CommandParameter="{Binding SelectedWorkflow}"
                            ToolTip.Tip="{loc:Localize WorkflowDelete}">
                        <ui:FASymbolIcon Symbol="Delete" />
                    </Button>
                </Grid>

                <TextBlock Text="{loc:Localize WorkflowTrigger}" Margin="0,10,0,0" FontWeight="Bold" FontSize="18" VerticalAlignment="Center" />
                <Grid ColumnDefinitions="*,auto">
                    <Button HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Padding="12,8">
                        <Button.Flyout>
                            <MenuFlyout ItemsSource="{x:Static services:WorkflowService.AllTriggers}">
                                <MenuFlyout.ItemContainerTheme>
                                    <ControlTheme TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}" x:DataType="workflows:WorkflowTriggerInfo">
                                        <Setter Property="Header" Value="{Binding DisplayName}"/>
                                        <Setter Property="Command" Value="{Binding $parent[UserControl].DataContext.SelectedWorkflow.SetTriggerInfo}"/>
                                        <Setter Property="CommandParameter" Value="{Binding}"/>
                                    </ControlTheme>
                                </MenuFlyout.ItemContainerTheme>
                            </MenuFlyout>
                        </Button.Flyout>
                        <Panel>
                            <TextBlock Text="{loc:Localize WorkflowSelectTrigger}" IsVisible="{Binding SelectedWorkflow.SelectedTriggerInfo, Converter={x:Static ObjectConverters.IsNull}}" />
                            <TextBlock Text="{Binding SelectedWorkflow.SelectedTriggerInfo.DisplayName}" />
                        </Panel>
                    </Button>
                    <Button IsVisible="{Binding !SelectedWorkflow.IsTestRunning}" Command="{Binding TestRunWorkflow}" CommandParameter="{Binding SelectedWorkflow}" Grid.Column="1" VerticalAlignment="Stretch" Margin="5,0,0,0"
                            ToolTip.Tip="{loc:Localize WorkflowTestRun}">
                        <ui:FASymbolIcon Symbol="Play" />
                    </Button>
                    <Button IsVisible="{Binding SelectedWorkflow.IsTestRunning}" IsEnabled="False" Grid.Column="1" VerticalAlignment="Stretch" Margin="5,0,0,0"
                            ToolTip.Tip="{loc:Localize WorkflowTestRunning}">
                        <ui:FASymbolIcon Symbol="Refresh" />
                    </Button>
                </Grid>
                <Border Background="{DynamicResource DockTitleBackgroundColor}" Margin="5" Padding="5" CornerRadius="4">
                    <ContentControl Content="{Binding SelectedWorkflow.Trigger.DisplayControl}" />
                </Border>

                <TextBlock Text="{loc:Localize WorkflowAction}" Margin="0,10,0,0" FontWeight="Bold" FontSize="18" VerticalAlignment="Center" />
                <Border Background="{DynamicResource DockTitleBackgroundColor}" Margin="5" Padding="5" CornerRadius="4">
                    <ContentControl Content="{Binding SelectedWorkflow.Actions.DisplayControl}" />
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
